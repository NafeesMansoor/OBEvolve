/**
 * OBEvolve's product version — shown in the footer (src/components/footer.tsx).
 * Bump this AND package.json's "version" field together; there's no build
 * step wiring them to each other automatically. Backend counterpart:
 * backend/app/core/config.py's Settings.app_version.
 */
export const APP_VERSION = '1.0.1'
