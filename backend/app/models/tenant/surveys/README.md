# Phase 7 — Surveys

Not yet implemented. See `docs/DATABASE_PLAN.md` §J for the planned schema
(`survey_templates`, `survey_questions`, `survey_instances`, `survey_responses`,
`survey_answers`).

This package exists now only so the module layout under `app/models/tenant/`
is stable for future phases; it contains no models yet.
