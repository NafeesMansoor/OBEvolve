# Local-dev override for testing the "demo" tenant — loaded via `--mode demo`
# alongside the base .env (this file's values take precedence). See .env for
# what these do.
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_INSTITUTION_SLUG=demo
