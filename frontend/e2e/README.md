# End-to-end tests

Playwright E2E tests will be added once there's a real flow worth testing
end-to-end (Phase 2+, once curriculum/outcomes/assessment UI exists on top
of the Phase 1 auth shell). Writing specs against the current login +
placeholder-dashboard UI would mostly assert against fixtures rather than
product behavior, so this directory intentionally stays empty for now.
