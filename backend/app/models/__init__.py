"""SQLAlchemy models, split by schema (see app/db/base.py):

- `app.models.public`  — tables in the shared `public` schema.
- `app.models.tenant`  — tables in each institution's `tenant_<slug>` schema.
"""
